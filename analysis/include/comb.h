#ifndef __COMBINATORICS
#define	__COMBINATORICS

#include "common.h"

UINT32 next_combination(UINT32 *ar, UINT32 n, UINT32 k);
UINT32 next_permutation(UINT32 *ar, UINT32 len);
UINT32 next_k_permutation(UINT32 *ar, UINT32 n, UINT32 k);

#endif

